# Life-OS 需求规格文档 (PRD) v3.0

## 1. 产品哲学

**"高杠杆，低负荷"**。Life-OS 不是一款简单的工具箱，而是一个基于 Mac 本地架构的**个人认知运行系统**。它通过严格的约束机制（Constraints）和极简的录入摩擦（Zero-Friction），防止用户在战术上的勤奋中迷失方向，确保所有行为皆对齐宏观的人生北极星。

**核心设计张力与解法**：系统里多处存在"记录要快"与"内容要有质量"的天然矛盾（比如工作复盘、交易复盘、知识沉淀）。统一解法是**分层处理**——当下的记录环节永远保持零摩擦（单行指令、点选即走），质量筛选环节挪到固定的周期性节点（如每周六），两者互不干扰。

---

## 2. 视觉设计系统

**基调**：macOS Clean Light Glass —— 整体低饱和柔和基底 + 高饱和醒目预警，兼顾长时间驻留桌面的舒适度与紧急信息的辨识度。

| 用途 | 色值 | 说明 |
| :--- | :--- | :--- |
| 背景衬底 | `#F8FAFC` | 通透浅灰 |
| 卡片背景 | `#FFFFFF` (75% + 毛玻璃模糊) | 高亮卡片 |
| 边框 | `#E2E8F0` | 1px 细边 |
| 主强调色 | `#0EA5E9` / `#38BDF8` | 天空蓝 / 冰爽蓝，平静低压迫 |
| 正文字色 | `#334155` | 低饱和石板灰 |
| 🚨 高优先/止损预警 | `#F43F5E` (Crimson Rose) | 高饱和，脉冲动效 |
| ⚠️ 次级预警 | `#F59E0B` (Amber) | 高饱和 |
| ✅ 完成/正向 | `#10B981` (Emerald) | 高饱和 |
| 数据/等宽字体 | JetBrains Mono | 金额、倒计时、代码类信息 |

**设计逻辑**：平时界面绝不刺眼，只有真正需要立刻处理的事情才会用高饱和色打断你的注意力。

---

## 3. 系统核心架构：九大模块

系统采用"顶层设计 + 底层执行"的联动架构：

| 模块 | 核心功能 | 约束哲学 |
| :--- | :--- | :--- |
| 🧭 人生地图 (Compass) | 领域原则、阶段北极星、演进日志 | 战略对齐，做人/做事双核 |
| 🏠 今日总览 (Overview) | 焦点与预警看板 | 每日聚焦，信息过滤 |
| 💼 工作 (Work) | 四象限攻坚、EOD 复盘、加班监测 | 高杠杆，防止发散 |
| 📅 日程 (Schedule) | 唯一排期权、日/周/月视图 | 时间块占用，防冲突 |
| 💰 财务 (Finance) | 记账、订阅、预算防超支 | 强制替换，防囤货拦截 |
| 📦 物品 (Items) | 极简库存、周期保养、使用时期临期提示 | 一进一出，极简空间 |
| 🤝 社交 (Network) | 人物名片聚合、重要日期提醒 | 真诚记录，不做断联量化 |
| 📈 投资 (Trade) | 观察列表、SOP 约束、持仓预警 | 纪律执行，防冲动交易 |
| 📚 学习 (Read & Inbox) | 领域 Roadmap、闪念与工作收获沉淀 | 结构化沉淀，闪念闭环 |

---

## 4. 零摩擦录入协议 (Command Palette)

### 4.1 交互流程（常驻式，无跳转）

`⌥ Space` 唤醒后是**单层面板**，不存在"下一步/上一步"的页面跳转：

```
Option + Space 唤醒
    ↓
面板顶部：输入框常驻显示（可直接打字，也可留空等待下方分类填充）
面板下方：分类胶囊按钮常驻显示，不会被替换或隐藏
    ↓
点击某个分类胶囊
    ├─ 无子类型 → 直接把示例模板填入顶部输入框，该分类胶囊高亮回显选中状态
    └─ 有子类型（如"工作""财务-支出"）→ 在分类胶囊下方新增一块子类型区域
         （主分类胶囊原样保留，不消失、不需要"返回"）
         点击子类型 → 填入对应模板，子类型胶囊高亮回显
    ↓
在示例上直接改写 → 回车提交，面板瞬间消失
```

**核心交互原则**：
- **无跳转**：输入框位置从头到尾不变，分类区域只做"增补"（追加子类型），不做"替换/切页"
- **分类常驻**：想换一个分类，直接点另一个胶囊按钮即可，不需要先点"返回"
- **选中回显**：点击过的分类胶囊、子类型胶囊都会高亮（浅蓝底 + 深蓝字），清楚显示当前选中的是哪一项
- **胶囊化视觉**：分类按钮做成小号胶囊（圆角药丸形，图标+文字横排），而非早期方形大图标块，视觉上更轻量、更适合频繁扫视

### 4.2 各分类示例模板

| 分类 | 预填示例模板 |
| :--- | :--- |
| 💼 工作 - 任务（子类型：Q1/Q2/Q3/Q4） | `[任务名] @[优先级] #[项目]` |
| 💼 工作 - EOD | 独立入口，直接跳转至工作模块内的 EOD 三输入框表单，不走示例改写流程 |
| 📅 日程 - 生活事件 | `[日期/时间] [事件名]` |
| 💰 财务 - 支出（子类型：必要支出/非必要支出） | `-[金额] [备注]`；非必要支出额外带 `#非必要，待月末评估` 标记 |
| 📦 物品 | `[物品名] [库存/操作]` |
| 🤝 社交 | `@[姓名] [互动内容/偏好]` |
| 📈 投资 - 每日复盘 | `[大盘/持仓观察] [有无操作及逻辑]` |
| 📚 学习 - 闪念 | `[随手记内容]` |

**设计说明**：所有录入统一走点击流程，不为高频操作（如财务记账）设计跳过通道——用户明确表示点击选择本身已经足够快，一致性优先于极致速度。

**与模块内录入入口的关系**：全局命令行（`⌥ Space`）是**快速录入的一种方式**，并非唯一入口。每个模块页面内部都有自己独立的录入/编辑入口（人生地图的原则录入 + 演进日志录入、工作模块各象限的添加按钮、日程待排期池的添加按钮、财务的快捷记账表单、物品的添加表单、社交的添加联系人表单、投资观察列表/持仓的录入表单、学习模块的添加表单等），用户可以在浏览某个模块时直接就地添加内容，不必先唤醒命令行再选择分类。

**人生地图不再出现在全局命令行分类中**：人生地图的原则、北极星、演进日志等录入完全由模块自身的原地表单承担，全局命令行的分类列表里不再包含"人生地图"选项。

**原地编辑（In-place Editing）原则**：所有已录入内容的修改，统一采用"点击文字直接变为可编辑输入框，失焦或回车即保存"的原地编辑交互，而不是弹出对话框（如系统 `prompt()`）或跳转到单独的编辑页面。这一原则贯穿人生地图原则/演进日志、物品名称与详情、社交名片信息、投资观察列表的价格字段、财务预算额度等所有可编辑内容——全系统统一手感，不存在"有的地方点按钮弹窗改、有的地方直接点字改"的不一致体验。

---

## 5. 各模块详细设计

### 5.1 🧭 人生地图 (Compass)

**分类维度**：顶层采用"**Being / Doing**"双核二分（UI 界面仅显示英文标签，不叠加中文注释）：
- Being：社交、物品、学习、爱好 四领域的修己准则
- Doing：工作、财务、投资、日程 四领域的达物法则

**三层结构**：

1. **核心原则与底线 (Principles)**：按 8 大执行领域挂载，作为日常行为防护栏
   - `content`（原则内容）、`domain`（所属领域）、`is_pinned`（是否置顶）

2. **阶段性北极星 (Strategic Milestones)**：按"做人/做事"归集，落脚到具体领域指标
   - `timeframe`、`title`、`status`（进行中/已达成/已废弃）、`associated_domains`

3. **个人演进日志 (Life Changelog)**：三维模型——**版本时间轴 + 演进类型 + 领域标签**
   - 演进类型分 4 类：🧠 认知重塑 / 🎯 战略转向 / ⚠️ 交租教训 / 🏆 重大突破
   - 每条日志可挂载 1-2 个领域标签
   - **投资平仓复盘、工作 EOD 中的重大教训均可一键沉淀至此**
   - **录入入口**：人生地图页面内置独立的"添加演进日志"入口（点选演进类型 → 填写关联领域 → 填写内容），原则录入同样在 Being/Doing 各自列内提供独立的原地表单（填写领域 + 内容），均不依赖全局命令行录入；已有内容支持原地编辑

**预警**：连续 14 天工作/日程任务均未关联北极星，触发 Amber 目标偏离提醒。

---

### 5.2 🏠 今日总览 (Overview)

汇总呈现：今日待办列表（时间 + 可直接勾选完成，默认展示当前 3 个未完成项）、耗材/预算/持仓预警、人生地图当前重心 Banner。仅做信息聚合，不承担数据录入职责。

**快捷录入的可用范围**：命令行录入入口（`⌥ Space` / 顶部"快捷录入"按钮）为**全局常驻**，在任意模块页面下都可直接唤醒，不局限于今日总览页。

**Mac 系统顶部菜单栏（独立于 App 内部界面）**：
- 常驻显示：抬眼可见下一个日程事件的时间与标题 + 预警红点
- 内置快捷录入入口，点击即可唤醒命令面板，无需先打开主窗口
- 点击日程摘要区域，弹出轻量 Popover：展示今日待办 mini 列表 + 预警 mini 列表，可勾选完成，并提供"展开完整视图"链接跳转主窗口
- 定位：让用户无需打开完整 App，仅靠系统菜单栏即可完成日常大部分"抬眼查看 + 快速录入"操作

---

### 5.3 💼 工作 (Work)

**核心哲学**：只回答两个问题——"今天最需要攻坚的 1~3 件事是什么" + "关键项目进度到哪了"。

**任务分类**：创建任务时先选"工作"分类，再**手动点选**归属四象限：
- Q1 重要且紧急（同时不超过 2 件）
- Q2 重要不紧急（黄金攻坚区）
- Q3 紧急不重要
- Q4 不紧急不重要（超 7 天未动，系统提示评估/删除）

**今日 Top3 焦点**：从 Q1 + Q2 任务池中**手动挑选** 1-3 件放入"今日"状态槽，Q3/Q4 不进入候选范围。

**项目**：轻量项目字段（名称 + 一句话目标），进度**手动填百分比**。

**北极星关联**：任务是否推进人生地图北极星，**手动勾选**，不自动继承。

**EOD 日终总结（三输入框，独立表单入口）**：

录入不再走单行指令，而是工作模块内的一个专属表单，包含：
- **加班时长**（独立数字输入框）
- **完成**（今日完成内容，文本框）
- **明天计划**（明日计划，文本框）
- **收获**（今日收获，文本框）

- 「明天计划」：**当天直接**生成次日待办（不等待筛选）
- 「收获」：**进入待筛选池**，不直接同步学习模块，等待每周六筛选
- 「加班时长」：累计计入本周工时统计

**EOD 历史时间轴**：工作模块内可直接**回顾过往每日的 EOD 记录**，按日期倒序排列的时间轴形式展示每条历史记录的加班时长、完成内容、明日计划与收获，方便随时翻看过去的工作节奏与思考轨迹。

**预警**：
- 🚨 P0 延期告警：Q1 任务超期触发 Crimson Rose 脉冲
- ⚠️ Dormant Inbox：任务停留超 7 天未处理，触发 Amber 告警
- ⚠️ 加班监测：周累计加班超 15 小时，触发 Amber 告警

---

### 5.4 📅 日程 (Schedule)

**核心原则（关键架构决策）**：日程模块拥有**唯一的"分配具体时间"权限**。工作、学习、爱好、物品、社交五类模块只负责**生产任务/提醒**，不产生时间；生活日程（惯例 + 额外安排）是唯一例外，因其本身即为"某时间点发生的事"，可直接在日程模块创建。

**任务流转规则**：

1. **先记录、后排期**：工作/学习/爱好/物品（补货保养提醒）/社交（重要日期提醒），这五类来源均在各自模块产生"未排期"状态的任务
2. **待排期池双向可见**：既能在各自模块（如工作 Inbox）看到，也能在日程模块的"待排期池"里统一看到
3. **排期动作**：用户在日程模块手动将待排期任务**拖入**具体时间格，获得 `scheduled_start_time` / `scheduled_end_time`
4. **状态双向同步**：任务一旦排期，原模块中显示"已排期 today 14:00"；完成状态无论在日程模块还是原模块勾选，两边**必须一致**，不允许数据分裂

**数据结构原则**：任务本身只有一份数据（存于所属模块表），日程模块**不维护副本**，仅通过 `source_module` + `source_task_id` + 排期时间字段引用它。

**视图**：日 / 周 / 月三维视角切换。

**预警**：
- 🚨 临近事件强提醒（开始前 15 分钟）
- ⚠️ 当日排期总时长超 8 小时，触发过度排班告警

---

### 5.5 💰 财务 (Finance)

**核心哲学**："零摩擦随手记，自动化防漏财"。

**数据结构**：收支明细（Transactions）、周期订阅（Subscriptions）、月度预算（Monthly Budget）。

**模块内独立录入入口（快捷记账）**：财务模块页面内置独立的记账表单，不依赖全局命令行录入——包含金额、备注两个输入框，以及"必要支出 / 非必要支出"点选标签，提交后直接生效或进入待评估队列（见下）。

**月度预算可编辑**：预算上限支持在财务模块内直接点击编辑，随时调整本月预算金额，进度条与预警状态实时联动重新计算。

**消费克制机制（Anti-Hoarding & One-In-One-Out）**：

1. **服饰/数码"一进一出"**：录入服饰类支出时，弹出轻量选择卡，要求指定 1 件旧物标记为已清理/闲置，否则标记为"未平衡冲动消费"
2. **耗材"防囤货"拦截 + 学习期设计**：
   - 冷启动阶段（样本 < 2-3 次）：首次触发需**手动标记**"这个东西快用完了"，系统记录触发时间与使用起点
   - 积累样本后：根据历史消耗速率自动推算库存周期，库存充足时拦截促销囤货需求，触发 Amber 警告
   - 冷启动期间样本不足，**宁可不报警，也不误报**，保护用户对该功能的信任
3. **非必要支出"延迟至月末评估"**：
   - 记账时标记为"非必要支出"的条目，**不立即计入本月已用预算**，而是进入"待评估队列"暂存
   - 到月底统一结算：比较当月预算结余与队列内非必要支出总额——**结余充足则统一放行**，计入当月消费；**结余不足则不放行**，建议顺延至下月重新评估是否购买
   - 目的：避免"当下觉得便宜/想买"的冲动即时兑现，把决策窗口拉长到月末，用结果（是否真的有余钱）倒逼消费克制

**预警**：
- 🚨 预算超支：达到 85% 时进度条转 Amber，达到 100% 时触发 Crimson 强告警
- ⚠️ 订阅续费提前 3 天提醒

---

### 5.6 📦 物品 (Items)

聚焦 4 类对生活/钱包有实质影响的物品：高频消耗品、定期保养品、**有固定使用时期的物品**、重要固定资产。

**使用时期与临期提示（新增维度）**：
- 与"消耗品"按消耗速率预测不同，部分物品（如护肤品、药品、化妆品）开封后有**固定的使用时期**（如"开封后 3 个月"），与用量无关，到期即视为失效
- 数据字段：`opened_at`（开封/启用日期）、`shelf_life_days`（使用时期天数）
- 临期提示按剩余天数分级：**≤7 天触发 Crimson 强告警**，≤30 天触发 Amber 提醒，其余正常展示剩余天数
- 系统内维护一个"临期提示队列"，汇总所有此类物品的到期倒计时，方便统一查看

**模块内独立录入与编辑入口**：物品模块页面内置独立的添加表单（物品名称 + 类型点选：消耗品/保养品/使用时期品/固定资产 + 关键信息），不依赖全局命令行录入；每个已有物品卡片也自带编辑入口，可随时就地修改名称与关键信息。

**任务流转**：物品的补货/保养/临期提醒均遵循"先记录后排期"规则——系统只生成一条"待处理提醒"，**不自动占用日程时间格**，由用户在日程模块手动决定何时安排（如采购、更换、丢弃）。

**数据补记支持**：`logged_at`（实际记录时间）与 `occurred_at`（事件实际发生时间）分离，允许断档补记，不污染当天统计口径。

---

### 5.7 🤝 社交 (Network)

**核心哲学**：真诚记录，**不做关系量化打分**（不设权重、不设断联倒计时）。

**架构**：以"人物名片 (Person Profile Card)"为唯一聚合载体，每人一张名片，包含：
- 偏好与避讳笔记（随手记录，不做打分）
- 互动历史（最后一次沟通内容）
- 重要日期子表 `important_dates`：
  - `person_id` / `event_name`（生日/手术/纪念日等）/ `date` / `is_recurring`（是否每年重复）/ `remind_days_before`

**提醒机制**：仅基于**具体日期**触发（生日、自定义重大事件），**不设"多久未联系"的断联雷达**，避免把人际关系数字化带来的功利感。

**模块内独立录入与编辑入口**：社交模块页面内置独立的"添加联系人/记录互动"表单（姓名 + 关系 + 偏好/互动内容 + 可选重要日期），不依赖全局命令行录入；每张已有名片也自带编辑入口，可随时就地更新姓名、关系近况与偏好笔记。

**任务流转**：重要日期提醒同样遵循"先记录后排期"——系统提示"某人生日快到了"，用户手动在日程模块安排具体的问候/礼物准备时间。

---

### 5.8 📈 投资 (Trade)

**核心定位**：交易纪律与风控引擎，不做行情分析工具。

**SOP 强约束（数据库层面）**：`trade` 持仓记录的 `watchlist_id` 字段设为 `NOT NULL REFERENCES watchlist(id)`，从数据结构上杜绝跳过观察列表直接建仓。

**三大功能**：

1. **观察列表 (Watchlist)**
   - **模块内独立录入与编辑入口**：观察列表内置"加入观察列表"表单（代码、名称、目标价、安全价、现价），不依赖全局命令行录入；每支已加入的股票也自带编辑入口，可随时调整目标价/安全价/现价
   - 支持后续手动调整目标价、安全价（变化即视为一次研究更新）
   - 支持 Markdown 研报上传与查看
   - **现价机制**：后台定时轮询新浪行情接口（仅交易时段内），现价数据自动更新
   - **逐股预警（而非整份观察列表告警）**：预警状态挂在每一支股票自己的行上，不影响列表内其他股票的显示：
     - 现价 **≥ 目标价** → 该股票行触发 🔴 **红色预警**（提示止盈/离场时机到了）
     - 现价 **≤ 安全价** → 该股票行触发 🔵 **蓝色预警**（提示进入安全边际买点）
     - 其余情况正常展示为"观察中"，无特殊高亮

2. **持仓管理 (Positions)**
   - **模块内独立录入入口**：持仓管理内置"分批建仓"表单，标的通过下拉选择框**仅能从观察列表中选取**（强约束在交互层面同步体现，而不仅是数据库层面），填写建仓价、止损价即可提交
   - 强制约束：只能从观察列表中选股建仓
   - **支持分批建仓/减仓**，系统自动计算持仓均价
   - 根据买入价/现价给出止损/止盈提示
   - **不设追高冷静期**——观察列表这道闸门已足够，无需额外摩擦层

3. **每日复盘 (Daily Review)**
   - 颗粒度为**按天**，不绑定具体交易动作——不管当天有无操作都可记录
   - 零摩擦单行指令：`trade review [大盘/持仓观察] [有无操作及逻辑]`
   - 若当天有交易动作（加仓/减仓/平仓），**自动关联**当天交易明细一并展示
   - 复盘时间轴按日期排列，点开某天可见"当天思考 + 交易记录"合并视图

**联动**：平仓重大教训可一键沉淀至【人生地图 Life Changelog】的"⚠️ 交租教训"类型。

---

### 5.9 📚 学习与闪念 (Read & Inbox)

**四大能力**：学习领域设定、学习计划(Roadmap)、学习任务、进度记录。

**模块内独立录入入口**：学习模块页面内置独立的"添加"表单，通过点选类型（学习领域 / 学习任务 / 闪念）切换录入对象，不依赖全局命令行录入：
- 选择"学习领域"：填写领域名称，新建一张领域进度卡片
- 选择"学习任务"：填写任务名称 + 可选关联领域，进入学习任务待排期池
- 选择"闪念"：填写内容，直接进入 Read & Inbox

已有的领域标题、Read & Inbox 条目均支持原地编辑。

1. **学习领域 (Learning Domains)**：定义 3-5 个主攻领域

2. **Roadmap（里程碑拆分）**：每个领域下设多个阶段性里程碑（如"第一阶段：搞懂IPC" → "第二阶段：搭建插件系统"），进度按里程碑完成状态反映，非简单百分比

3. **学习任务**：遵循"先记录后排期"规则——创建时不带时间，进入学习模块待办池，由日程模块负责排期

4. **进度呈现**：
   - 每个领域独立展示里程碑进度
   - 本周整体投入统计（完成学习任务数 / 累计学习时长）

**Read & Inbox 沉淀表（合并结构）**：
- 闪念胶囊（随手记录的碎片想法）与 EOD 收获（周六筛选后的工作复盘沉淀）**合并为同一张表**
- 用 `source` 字段区分来源（`inbox_flash` / `work_eod`），呈现时可混排也可按来源筛选
- 每条记录可选关联所属学习领域（非必填）

---

## 6. 跨模块联动规则总览

| 联动 | 规则 |
| :--- | :--- |
| 工作 EOD → 学习模块 | "明天计划"字段当天直接生成待办；"收获"字段进入待筛选池，每周六批量筛选后打标沉淀 |
| 五类任务来源 → 日程 | 工作/学习/爱好/物品/社交 均"先记录、日程负责排期"，任务数据不重复存储，日程模块仅引用 |
| 日程 ↔ 原模块 | 排期状态与完成状态双向同步，禁止数据分裂 |
| 投资平仓/工作 EOD → 人生地图 | 重大教训/突破可一键沉淀至 Life Changelog |
| 财务 → 物品 | 记账时触发一进一出/防囤货检查，物品库存联动更新 |
| 财务 → 学习领域 | Read & Inbox 沉淀条目可选关联学习领域，非强制 |

---

## 7. 数据结构模型 (SQLite，概要)

- **Person**：人物名片核心表，聚合偏好、互动历史；子表 `important_dates`（生日/重大事件，含 `is_recurring`、`remind_days_before`）
- **Task/Event**：支持工作、学习、爱好、生活、物品、社交 6 源标签；含 `is_scheduled`、`scheduled_start_time`、`scheduled_end_time`、`source_module`、`source_task_id`
- **Transaction**：预算关联、自动扣费预警、一进一出替换检查字段、`is_necessary`（必要/非必要）、`pending_settlement`（是否处于待月末评估状态）
- **MonthlyBudget**：`budget_limit` 支持随时手动编辑更新
- **Item**：库存水位、消耗周期（含学习期样本计数）、定期维护计划、`opened_at` / `shelf_life_days`（使用时期临期）、`logged_at` / `occurred_at` 分离
- **Watchlist / Position**：`position.watchlist_id NOT NULL REFERENCES watchlist(id)`（数据库级强约束）
- **DailyReview（投资）**：按日期为主键，关联当日交易明细
- **ReadInboxEntry**：`source` 字段区分闪念/工作收获，可选 `domain_id` 关联学习领域
- **LearningDomain / Milestone**：领域下挂里程碑列表，完成状态驱动进度

---

## 9. 易用性与视觉风格

### 9.1 视觉识别系统

- **整体基调**：macOS Clean Light Glass，低饱和柔和基底 + 高饱和醒目预警（见第 2 节配色表），长时间驻留桌面不刺眼，但紧急信息一眼可辨
- **无渐变色**：全系统不使用任何渐变背景/渐变按钮/渐变头像，所有色块均为纯色平铺，视觉更克制、更"工具感"而非"营销感"
- **无边框线，用颜色区分边界**：卡片、按钮、输入框一律不使用描边（border），区域之间的边界完全靠背景色深浅、色块本身的色彩差异来划分（例如四象限用四种浅色背景区分，而非加边框）
- **图标系统**：全站统一使用一套简约线条图标（home / compass / briefcase / calendar / wallet / package / users / trending-up / book-open / zap / check-square / alert-triangle / plus / edit 等），同一粗细、同一风格，默认灰色扁平，仅在选中态才切换为主题蓝色，不使用彩色表情符号作为界面图标（复选框符号 ☐/☑ 除外，因其本身即为单色符号）
- **预警脉冲效果统一**：所有触发预警的元素（今日总览的预警聚合卡片、投资观察列表逐股预警行）都使用同一套呼吸脉冲动画——Crimson 高优先/止盈预警用红色脉冲，安全边际买点等正向提醒用蓝色脉冲，视觉语言全站一致，用户一眼就能识别"这是同一类事情"

### 9.2 交互一致性原则

- **模块化录入 + 全局录入并存**：`⌥ Space` 全局命令行是"快速录入的一种方式"，但不是唯一方式——九大模块每一个都有自己独立的、常驻页面内的录入表单，用户在任意模块里都能就地添加内容，不必依赖跳出当前上下文
- **原地编辑，拒绝弹窗**：所有已录入内容的修改都采用"点文字直接变输入框"的原地编辑手感，不使用系统级 `alert/prompt` 弹窗，也不跳转到单独的编辑页，保证心流不被打断
- **命令面板无跳转**：分类选择常驻于输入框下方，不做分栏跳转或"下一步/返回"式导航；有子类型的分类在原位追加子选项区域，输入框位置全程不变
- **选中状态可回显**：命令面板里任何一次点击（无论是主分类还是子类型）都会给出明确的高亮反馈，让用户清楚"系统记住了我刚才点的是什么"，避免"点了但不知道有没有生效"的疑虑
- **预警颗粒度对齐真实决策单元**：预警状态挂载在真正需要单独判断的最小单元上（例如投资预警落在每一支股票的行上，而不是让整份观察列表一起红），避免"一个东西报警，牵连一整块内容"的误导

### 9.3 高频操作路径优化

- 今日总览的待办列表默认只展示 3 个未完成项、支持直接点击打勾，减少不必要的信息噪音
- Mac 系统菜单栏常驻显示下一个日程与预警红点，配合独立的快捷录入入口，让"抬眼查看 + 随手记一笔"这类最高频操作完全不需要打开完整 App 主窗口
- EOD、每日交易复盘均保留"当天必须能几秒内记完"的最低摩擦路径（EOD 三个输入框 + 独立数字框；交易复盘为单行文本入口），复杂的筛选/沉淀/结算动作统一挪到固定的周期性节点（每周六、每月底），不占用日常记录的心智带宽

---

## 10. 开发路线图 (MVP，待细化)

> 本阶段划分为早期草案，尚未结合本次核心机制确认结果重新排期，仅供参考：

1. **阶段 1：全局壳子**（Mac Menu Bar + Command Palette 点选式交互 + 本地数据存储）
2. **阶段 2：核心执行层**（工作四象限、日程排期引擎、Dashboard）
3. **阶段 3：防漏与纪律层**（财务预算、物品库存预警、投资 SOP）
4. **阶段 4：认知与演进层**（人生地图、学习模块、知识沉淀）

**待办事项**：需重新评估阶段 2 中 Dashboard 与阶段 3 依赖数据（财务/物品/投资预警）之间的顺序冲突，以及日程排期引擎（跨模块引用架构）作为核心基础设施应提前到阶段 1 末端验证。
