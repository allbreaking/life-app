{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum permissions for the local Life-OS window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
